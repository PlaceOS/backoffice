import { DriverFormatPipe } from './driver-format.pipe';

describe('DriverFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DriverFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
